
	{
		"percentChange": 
		{
			"script": "v1 = (\$ in _source.priceHistory if \$.date == from); v2 = (\$ in _source.priceHistory if \$.date == to); return v2.size() > 0 && v1.size() > 0 ? v2[0].value - v1[0].value",
			"params": 
			{
				"to": $to$,
				"from": $from$
			}
		}
	}
